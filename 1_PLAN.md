## 背景
- 現在Cursorで実施しているコーディングタスクをPi上でワークフロー的に実行させたい

## ワークフローの大雑把なイメージ
1. Linearから指定されたタスクを1つ取り出す
2. (賢いモデルで) Linear の内容を精読し、曖昧なところがないか判断し、もし曖昧であればユーザに突き返す
  - 大事なのは要求(入出力の境界値)とずれないことであり、実装レベルは現場に任せたいので、一字一句矛盾を追求しなくて良い。
  - 要件などから推察できることは「推測した」旨を記述し進めて良い
3. (賢いモデルで)タスク実行のプランを行う
  - タスクは並列度を考えて切る
  - タスク管理はAgent + 開発者がローカルで読めれば良いためLinearに置く必要はない。Markdownなどで十分だが、Agent管理上必要であれば別の管理ソリューション入れても良い。
4. (コスト効率の良いモデルでモデルで)実装をSubagentで実施する
5. (賢いモデルで)全てのsubagentが完了したら検品をする
  - NGな場合、タスク管理に追加してコスト効率の良いモデルで修正を行う(これも複数ある場合は並列で行えると良い)
  - ある程度のループ数を決め、それを超える場合はユーザにエスカレーションする
  - 検品は重箱の隅をつつきすぎないようにすること
  - 例えば要求は満たせそうだがちょっと懸念があるがループ数が上限になったとかであれば一旦懸念はPRなどで表現すれば良い
  - 方向性として要求事項との不一致や重大なセキュリティホールのようなクリティカルなものは直すべきだが、ちょっとした記法ややや非効率な実装、かなり確率の低い懸念などはスルーで良い。特に効率性は無限に追求できるが結局動かしてみないと何が懸念になるかわからないため。記法は全体的な規則は揃えたいが、むしろLinterやtestの充実とそれをPassすることが大事。
6. OKならPRを作成する
  - PRがCIで動いている場合それをパスすることを絶対条件とする。必要ならこれもタスクを作り適合する形にコスト効率の良いモデルに直させる
7. CIがOKならマージする
8. マージ後、本番環境を確認し問題ないか確認する。
  - 問題がない場合、開発内容と懸念点があればまとめて起票して報告する
  - 問題を確認した場合、原因を調査しLinearチケットを起票し報告して終了する

## その他要件
- `extensions/profiler.ts` にあるようなものは絶対に入れてほしい。これを見て改善を回すのが目的であるため
- 環境によって7以降は変化する可能性が高い(レビュー必須の環境も多い)のでこの辺は外から制御できると嬉しい
- デフォルトで読み取れてしまうような余計なSKILLやtoolはロードしないようにしたい

## この先やりたいこと

- 適当なアプリを作る等してこの実装を試したい
- こういう改善できたらいいなの中身
    - Jevを使って不要なファイル呼び出しやツールコールの削除
    - だいたいLLMは日本語下手なのでGeminiを使った日本語改善
